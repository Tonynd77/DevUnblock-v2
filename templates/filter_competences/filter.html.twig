{% extends 'base.html.twig' %}

{% block title %}Hello FilterCompetencesController!{% endblock %}

{% block body %}
<style>
    .example-wrapper { margin: 1em auto; max-width: 800px; width: 95%; font: 18px/1.5 sans-serif; }
    .example-wrapper code { background: #F5F5F5; padding: 2px 6px; }
</style>

<div class="example-wrapper">
    {{form(form)}}
    {# <button type="input" class="btn btn-color-blue">
        Rechercher
    </button> #}
    <section class="container-fluid oneArticle m-5" style="min-height=100vh">
        {% for user in result %}
            
        {{ user.abonneDescription }}

        {% endfor %}
    </section>
{% endblock %}
